-- GUION PARA JUEGO POKER DE DADOS --

clase dado
	- Constructor para dado:
	    - atributos:
	        valores:    ["AS","K","Q","J","6","5"] ó [1,2,3,4,5,6]
	        valor:      valor aleatorio de valores
	- Getter y Setter
    * Métodos
	- tirada
	    valor aleatorio de valores

clase jugador
        importar dado
    - Constructor para jugador:
        nombre
        dado * 5
        puntos
        puntos por tirada
        tiro premiado # Si es premiado guardamos los valores que elija el jugador
    - Getter y Setter
    * Métodos
    Almacenar premios (poker real), (poker), (full), (escalera mayor), (escalera menor), (trio), (dobles), (pareja)
    - Marcador:

    - tira dados:

    - haz tirada:






